# PortLab — 个人投资分析工具箱

## 概述

PortLab 是一个个人投资分析平台，提供定投回测、收益追踪等功能，持续集成更多分析能力。

- **后端：** Python（数据拉取、计算）
- **包管理：** uv
- **数据库：** MySQL（存储所有原始数据、中间计算结果、最终输出）
- **部署：** Docker Compose 一键启动
- **前端：** 现代主流前端图表框架（推荐 ECharts，成熟稳定、金融图表支持好）

## 架构

```
┌─────────────┐     ┌──────────────┐     ┌─────────┐
│   Frontend   │────▶│   Backend    │────▶│  MySQL  │
│  (ECharts)   │◀────│   (FastAPI)  │◀────│         │
└─────────────┘     └──────┬───────┘     └─────────┘
                           │
                    ┌──────┴───────┐
                    │  DataFetcher  │
                    │  (数据拉取)    │
                    └──────────────┘
```

### 技术选型

| 组件 | 选择 | 理由 |
|------|------|------|
| 后端框架 | FastAPI | 异步、自带 Swagger、Python 生态 |
| 数据库 | MySQL 8.x | 成熟稳定，适合结构化金融数据 |
| ORM | SQLAlchemy 2.x | Python 主流 ORM |
| 前端图表 | ECharts 5.x | 金融图表能力强，文档完善，社区活跃 |
| 前端框架 | Vue 3 + Vite | 轻量、上手快、与 ECharts 配合好 |
| 包管理 | uv | 快速、现代化 |
| 部署 | Docker Compose | 单机一键启停，适合个人项目 |

### 数据流

1. **数据拉取层（DataFetcher）**：定时/手动从数据源获取标的行情数据，存入 MySQL `raw_*` 表
2. **计算层（Compute）**：基于原始数据执行回测/分析计算，中间结果存入 `calc_*` 表，最终结果存入 `result_*` 表
3. **API 层（FastAPI）**：提供 RESTful 接口，前端按需查询
4. **展示层（Frontend）**：ECharts 渲染可视化图表

### 数据库表命名约定

- `raw_*` — 原始拉取数据（如 `raw_price_daily`）
- `calc_*` — 计算中间结果
- `result_*` — 最终输出结果（供前端直接消费）

## 功能规划

### Phase 1：定投回测（MVP）✅ 已实现（v0.1.0）

> 实现状态：定投回测全链路（数据拉取 → 计算 → API → 前端可视化）已完成并通过端到端验证。

#### 功能描述

用户选择一个投资标的，设定定投频率（按周或按月）和每期金额，系统回测整个定投区间的市值变化，并以双轴图表展示结果。

#### 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| 标的代码 | string | 如基金代码、股票代码 |
| 定投频率 | enum | `weekly`（每周）/ `monthly`（每月） |
| 每期金额 | decimal | 每次定投投入金额 |
| 起始日期 | date | 回测开始日期 |
| 结束日期 | date | 回测结束日期（默认至今） |

#### 计算逻辑

1. 按定投频率生成投资日历（每期固定日期投入）
2. 每个投入日按当日标的价格买入，累计持有份额
3. 每个交易日计算当前持仓市值 = 累计份额 × 当日价格
4. 每个交易日计算：
   - 累计投入成本
   - 当前市值
   - 累计盈亏 = 市值 - 成本
   - 收益率 = 盈亏 / 成本 × 100%

#### 图表展示

**双轴折线 + 柱状组合图：**

- **横轴：** 时间轴（交易日）
- **左纵轴：金额（元）**
  - 市值变化曲线（主线）
  - 成本曲线（参考线）
- **右纵轴：收益率（%）**
  - 收益率曲线（副线，不同颜色）
- **柱状标记：** 每个数据点上用小直方条展示当期盈亏
  - 向上（正值，绿色）：盈利
  - 向下（负值，红色）：亏损

#### 输出指标

| 指标 | 说明 |
|------|------|
| 累计投入 | 总投入金额 |
| 当前市值 | 最终权益市值 |
| 累计收益 | 绝对收益金额 |
| 累计收益率 | 总收益率 |
| 年化收益率 | 折算年化 |
| 最大回撤 | 区间内最大回撤 |
| 定投期数 | 总投资次数 |

### Phase 2：红利 MA120 策略回测

> 与定投回测并列的独立回测工具，基于「红利ETF + MA120 均线」波段策略，支持固定本金、持续投入、混合三种资金模式。

#### 功能描述

用户选择一个投资标的（红利 ETF 等），设定资金模式和策略参数，系统按 MA120 均线触发买入/卖出信号，回测整个区间的市值变化，并以双轴图表展示结果。行情数据复用 `raw_price_daily` 已有数据，避免重复请求。

#### 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| 标的代码 | string | 如 ETF 代码、股票代码 |
| 起始日期 | date | 回测开始日期 |
| 结束日期 | date | 回测结束日期（默认至今） |
| 资金模式 | enum | `fixed`（固定本金分批）/ `recurring`（每月持续投入）/ `hybrid`（初始本金+每月追加） |
| 初始本金 | decimal | fixed / hybrid 模式下的初始资金总额 |
| 每月投入 | decimal | recurring / hybrid 模式下每月新增资金 |
| 份数 | int | 初始本金分成 N 份（默认 10） |
| MA 周期 | int | 均线周期，默认 120 |
| 起始买入阈值 | float | 跌破 MA × 此比例开始买入（默认 0.985） |
| 加仓步长 | float | 每跌此幅度加仓一份（默认 0.01） |
| 暴跌加倍倍数 | int | 暴跌时加倍加仓倍数（默认 2） |
| 卖出方式 | enum | `batch`（MA 上方分批卖出）/ `all`（全部卖出）/ `half`（卖半留底仓） |
| 分红处理 | enum | `cash`（现金领取）/ `reinvest`（分红复投） |

#### 策略逻辑

**买入规则（MA120 下方）：**
1. 价格跌破 MA120 × 0.985 时触发首笔买入
2. 资金按份数分批，每再跌 0.01 加仓一份
3. 遇暴跌（单日跌幅超阈值）按加倍倍数加仓
4. 不止损，越跌越买直到资金用完

**卖出规则（MA120 上方）：**
1. 价格站回 MA120 上方后按卖出方式执行
2. 分批模式：每涨一定幅度卖出一份
3. 全部模式：一次性清仓
4. 半仓模式：卖出 50%，留底仓等下次信号

**分红处理：**
- `cash`：分红以现金记入账户，不计入份额
- `reinvest`：分红按除息日收盘价折算份额，增加持仓

#### 计算逻辑

1. 从 `raw_price_daily` 读取标的行情数据（复用已有数据，缺失时自动补拉）
2. 逐交易日计算 MA120 并判断买入/卖出信号
3. 按资金模式和加仓规则执行买入
4. 按卖出方式和信号执行卖出
5. 每个交易日计算：累计投入、当前市值、持仓份额、现金余额、盈亏、收益率
6. 分红日（如有）按分红处理逻辑调整持仓

#### 图表展示

**双轴折线 + 柱状 + 标记组合图：**

- **横轴：** 时间轴（交易日）
- **左纵轴：金额（元）**
  - 市值变化曲线（含现金）
  - 累计投入成本曲线（参考线）
  - MA120 参考线（浅色虚线）
- **右纵轴：收益率（%）**
  - 收益率曲线
- **标记点：**
  - 🔴 红色标记 = 买入日
  - 🔵 蓝色标记 = 卖出日
- **柱状条：** 盈亏（盈绿/亏红）

#### 输出指标

| 指标 | 说明 |
|------|------|
| 累计投入 | 总投入金额 |
| 当前市值 | 最终权益市值 + 现金 |
| 累计收益 | 绝对收益金额 |
| 累计收益率 | 总收益率 |
| 年化收益率 | XIRR 折算年化 |
| 最大回撤 | 区间内最大回撤 |
| 买入次数 | 总买入操作次数 |
| 卖出次数 | 总卖出操作次数 |
| 分红累计 | 区间内累计分红金额 |
| 胜率 | 盈利卖出次数 / 总卖出次数 |

### Phase 3+（后续迭代）

- 多标的对比回测
- 持仓跟踪与实时盈亏
- 资产配置分析
- 投资组合绩效归因

## 项目结构（初始）

```
portlab/
├── docker-compose.yml
├── SPEC.md
├── TASKS.md
├── backend/
│   ├── pyproject.toml
│   ├── app/
│   │   ├── main.py              # FastAPI 入口
│   │   ├── config.py            # 配置管理
│   │   ├── database.py          # 数据库连接
│   │   ├── models/              # SQLAlchemy 模型
│   │   ├── schemas/             # Pydantic schemas
│   │   ├── api/                 # 路由
│   │   │   └── backtest.py
│   │   ├── services/            # 业务逻辑
│   │   │   ├── fetcher/         # 数据拉取
│   │   │   └── compute/         # 计算引擎
│   │   └── utils/
│   ├── tests/
│   └── Dockerfile
├── frontend/
│   ├── package.json
│   ├── src/
│   │   ├── views/
│   │   │   └── Backtest.vue
│   │   ├── components/
│   │   ├── api/
│   │   └── App.vue
│   ├── vite.config.ts
│   └── Dockerfile
└── mysql/
    └── init/
        └── 01_schema.sql        # 初始化建表
```

## 开发约定

- Python 代码遵循 PEP 8，使用 ruff 做 lint/format
- 前端使用 ESLint + Prettier
- 数据库变更通过 SQL migration 文件管理，不直接改库
- API 遵循 RESTful 规范，返回统一 JSON 结构
- 所有金额使用 Decimal 精度计算，避免浮点误差
